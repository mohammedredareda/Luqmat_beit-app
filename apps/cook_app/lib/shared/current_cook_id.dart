/// The cook currently signed in. Hardcoded because there is no
/// auth/session feature in this repo yet — swap for the real session's
/// cook id once auth exists (auth is CK-01–CK-05/CK-27, out of scope here).
const currentCookId = 'cook-1';

/// Display name for [currentCookId] — hardcoded alongside it until auth
/// exists (see [currentCookId]'s doc comment).
const currentCookName = 'مطبخ أم أحمد';
