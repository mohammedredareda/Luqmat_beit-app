name: luqmat_beit
description: "Melos workspace root for luqmat_beit (Home Chef App) — not a runnable app itself."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

# This root package is workspace tooling only (Melos). The two runnable apps
# are apps/customer_app and apps/cook_app; shared code lives in packages/core.
dev_dependencies:
  melos: ^6.1.0
